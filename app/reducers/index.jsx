import { combineReducers } from 'redux'

import campuses from './campuses'

const rootReducer = combineReducers({ campuses })


export default rootReducer

// export default combineReducers({campuses});
